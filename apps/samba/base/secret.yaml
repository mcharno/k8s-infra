apiVersion: v1
kind: Secret
metadata:
  name: samba-credentials
  namespace: samba
  labels:
    app: samba
type: Opaque
stringData:
  # ⚠️  WARNING: TEMPLATE FILE - DO NOT USE AS-IS! ⚠️
  #
  # This is a TEMPLATE with placeholder values.
  # You MUST change these values before deploying!
  #
  # For automated setup, use: ./scripts/setup-samba.sh
  #
  # For manual setup:
  #   1. Update the values below
  #   2. Apply: kubectl apply -f apps/samba/base/secret.yaml
  #   3. DO NOT commit your real credentials to git!

  # Username for Samba access
  username: "sambauser"

  # Password for Samba access (minimum 8 characters recommended)
  # ⚠️  CHANGE THIS PASSWORD!
  password: "CHANGEME_SecurePassword123"

  # User ID (should match host user if needed)
  userid: "1000"

  # Group ID (should match host group if needed)
  groupid: "1000"
